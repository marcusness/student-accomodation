{
  "name": "student-accomodation",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "build": "cd client && npm run build",
    "start": "cd server && npm start"
  },
  "dependencies": {
    "pdfkit": "^0.16.0"
  }
}
